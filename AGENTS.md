# seju-face-lab

Global baseline: `C:\Users\ogosh\AGENTS.md`.

Repo-specific rules:

- Keep face vectors and generated outputs local. Do not commit personal image datasets, generated portraits, embeddings, or model outputs.
- Use deterministic commands for verification; prefer `python -m unittest`.
- Avoid claiming the vector represents a real population. It is an approximation of the provided image set only.
